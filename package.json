{
  "name": "sovereign-council-irys-bundle",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tailwindcss -i ./global.css -o ./dist.css --minify && esbuild ./index.js --bundle --outfile=./irys-bundle.js --minify --format=iife"
  },
  "dependencies": {
    "@irys/web-upload": "^0.0.4",
    "@irys/web-upload-ethereum": "^0.0.4",
    "@irys/web-upload-ethereum-ethers-v6": "^0.0.4",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "esbuild": "^0.20.0"
  }
}